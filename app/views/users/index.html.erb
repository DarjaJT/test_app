<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %><!-- render - ищет партиал с названием _user.html.erb -->
</ul>

<%= will_paginate %>